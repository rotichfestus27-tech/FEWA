/*
  Client configuration example.
  Copy this file to `config.js` (do NOT commit `config.js`) and fill your Firebase web config.

  Example:
  window.FEWA_CONFIG = {
    firebase: {
      apiKey: "YOUR_API_KEY",
      authDomain: "YOUR_PROJECT.firebaseapp.com",
      projectId: "YOUR_PROJECT_ID",
      appId: "YOUR_APP_ID",
      measurementId: "G-XXXX"
    }
  };
*/

window.FEWA_CONFIG = {
    firebase: {
        apiKey: "REPLACE_ME",
        authDomain: "REPLACE_ME",
        projectId: "REPLACE_ME",
        appId: "REPLACE_ME",
        measurementId: "REPLACE_ME"
    }
};
